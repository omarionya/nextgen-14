version: '3.8'

services:

  web:
    build: .
    ports:
      - "8081:80"

    depends_on:
      - db

  db:
    image: mysql:8

    restart: always

    environment:
      MYSQL_ROOT_PASSWORD: password123

    volumes:
      - mysql_data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
   mysql_data:
